# 资料源指针目录

本目录只保存上游资料源指针，不保存 SEP-CN 正文副本。

默认约定：

```bash
git clone https://github.com/Rivensa/SEP-CN .source/SEP-CN
```

`source/SEP-CN` 是指向 `../.source/SEP-CN` 的软链接；当 `.source/SEP-CN` 尚未克隆时，该链接可以暂时不存在目标，这是预期状态。
